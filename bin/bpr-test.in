#!@SWIPL@ -q -t halt -g main -s 

file_search_path(bio,library('bioprolog')).

:- use_module(bio(bioprolog_util)).
:- use_module(library(readutil)).

main:-
        getopt([],[F]),
        statistics,
        f2a(F,A),
        atom_length(A,Len),
        writeln(Len),
        statistics,
        prolog.

f2a(F,A):-
        read_file_to_codes(F,CL,[]),
        atom_codes(A,CL).
